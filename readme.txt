About Holy Webhook:

Just a cool webhook spammer that spams webhooks

You need to install these packages: In command prompt inside the directory of the python script

type

py -m pip install json

py -m pip install requests

py -m pip install strings ( May already be installed )

you need python installed as well